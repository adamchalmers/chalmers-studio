@settings(defaultLengthUnit = cm)

import carveHole, holeProfile, drill, blind, noTopFeature from "hole_lib.kcl"

carveHole(
  holeProfile(
    bottom = drill(bitAngle = 118deg),
    body = blind(depth = 10, diameter = 4),
    top = noTopFeature(),
  )
)
